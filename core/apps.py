from django.apps import AppConfig


class CoreConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'core'

    # def ready(self):
    #     # Import signals and receivers here to ensure they are registered
    #     from . import signals
    #     from . import receivers  # Import your receiver functions





   
